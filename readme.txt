index.php - The file implements main app

autoloader.php - The file implements spl_autoload_register() function

./vendor - dir contains files that inherit core files and implement them.

./log - contains log file.

./app - dir contains main files for works app like interfaces, models and etc.